---
spring:
  application:
    name: video-platform
  servlet:
    multipart:
      max-file-size: 500MB
      max-request-size: 500MB
  jpa:
    hibernate:
      # Schema is owned by Flyway; Hibernate only checks the entities match it.
      ddl-auto: validate
      
minio:
  url: http://localhost
  port: 9000
  user: minio
  password: minio123
  bucket: videos

ffmpeg:
  path: ${FFMPEG_PATH:ffmpeg}

processing:
  poll-interval-ms: 5000

jwt:
  secret: 2FyufRkgccx+HkVvA/83x8B1dLFrD3LjCg+3IOJhsV0=
  expiration: 86400000