---
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/videoplatform
    username: video
    password: video
  jpa:
    properties:
      hibernate:
        format_sql: true
    show-sql: true
  flyway:
    # Adopt an existing local DB that predates Flyway instead of erroring on it.
    baseline-on-migrate: true

minio:
  url: http://localhost
  port: 9000
  user: minio
  password: minio123
  bucket: videos

jwt:
  # Dev-only throwaway secret so `mvn spring-boot:run -Plocal` works without any
  # env setup. This is NOT the production key and must never be reused there.
  secret: ${JWT_SECRET:Y9ckG0KOe2j3epTosmt0SU+3owH4i0nedSlspMxEtis=}
